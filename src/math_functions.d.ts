declare module 'math_functions' {
    export function calculateAngle(ax: number, ay: number, bx: number, by: number, cx: number, cy: number): number;
    export function calculateDistance(ax: number, ay: number, bx: number, by: number): number;
    export function getSide(ax: number, ay: number, bx: number, by: number, cx: number, cy: number): number;
}
